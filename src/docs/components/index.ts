import Error from './schemas/Error';
import AllowedUsers from './schemas/AllowedUsers';
import AllowedUser from './schemas/AllowedUser';
import LinkMode from './schemas/LinkMode';
import LinkMessage from './schemas/LinkMessage';
import Logs from './schemas/Logs';
import VCard from './schemas/VCard';
import Group from './schemas/Group';
import Groups from './schemas/Groups';
import Configs from './schemas/Configs';

export default {
  schemas: {
    Error,
    AllowedUsers,
    AllowedUser,
    LinkMode,
    LinkMessage,
    Logs,
    VCard,
    Group,
    Groups,
    Configs,
  },
};
